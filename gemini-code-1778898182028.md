# 角色与目标
你是一位资深的前端开发工程师。你的任务是根据以下设计规范，构建一个现代、干净、功能强大的“个人 AI 提示词管理与收集网站”（Personal AI Prompt Management Website）。
应用需要具备完全的响应式设计、优雅的视觉交互，并达到生产级代码标准。

---

## 1. 技术栈要求
*   **前端框架：** React (采用函数式组件与 Hooks)
*   **样式方案：** Tailwind CSS (现代、极简风格，需包含平滑的过渡动画)
*   **图标库：** Lucide React (或标准的 Tailwind 兼容 SVG 图标)
*   **状态管理与持久化：** 使用 React 的 `useState` 和 `useEffect` 结合 `localStorage` 进行本地数据持久化，确保应用开箱即用，暂不需要复杂的后端接口。

---

## 2. 核心功能点
1.  **提示词增删改查 (CRUD)：** 用户可以自由地创建、查看、编辑和删除提示词。
2.  **一键复制功能：** 每个提示词卡片上都有一个显眼的“复制”按钮，点击后将提示词文本复制到剪贴板，并提供即时的视觉反馈（如 “已复制！” 气泡提示或图标状态切换）。
3.  **实时搜索与筛选：** 
    *   支持按标题、描述或提示词具体内容进行实时的模糊搜索。
    *   支持点击标签（Tags）进行分类筛选。
    *   支持按“收藏/置顶”状态进行快捷筛选。
4.  **标签动态管理：** 从现有的提示词数据中动态提取去重后的标签，展示在侧边栏中，并显示每个标签下的提示词数量。
5.  **数据导入与导出：** 支持将所有提示词数据导出为 `.json` 文件备份；同时支持导入符合格式的 JSON 文件以恢复或同步数据。

---

## 3. 数据结构 (TypeScript 接口)
每个提示词对象需严格遵循以下数据结构：
```typescript
interface PromptItem {
  id: string;          // 唯一标识符（UUID 或时间戳）
  title: string;       // 提示词标题
  description: string; // 简短描述或使用场景备注
  content: string;     // 核心 AI 提示词具体文本
  tags: string[];      // 标签数组（例如：["写作", "编程", "营销"]）
  isFavorite: boolean; // 是否收藏 / 置顶状态
  createdAt: string;   // ISO 时间戳（创建时间）
  updatedAt: string;   // ISO 时间戳（更新时间）
}